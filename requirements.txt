# Experiment dependencies for the IAAI-27 supplementary code.

torch>=2.0
torchvision
pytorch-lightning
segmentation-models-pytorch
timm
transformers>=4.36
numpy
scipy
pandas
scikit-learn
scikit-image
opencv-python
Pillow
tqdm
PyYAML
matplotlib
tabulate

# Learned no-reference IQA metrics (CLIPIQA, MANIQA, MUSIQ)
pyiqa

# Classical descriptors (Zernike, BRISQUE, PIQE)
mahotas
brisque
pypiqe

# Finer-CAM qualitative analysis (Zhang et al., CVPR 2025,
# arXiv:2501.11309). Finer-CAM is merged into jacobgil/pytorch-grad-cam,
# which ships as `grad-cam` on PyPI. Verified against 1.5.5, whose
# `pytorch_grad_cam.FinerCAM` is what this code imports.
grad-cam>=1.5.5

# Deliberately NOT listed: scikit-video. Its classical NIQE calls
# scipy.misc.imresize, removed in SciPy 1.3, so the four NIQE columns are
# dropped rather than emitted as NaN. The paper's descriptor vectors are 936-d
# on that basis; installing a working NIQE changes the dimensionality.
